# 2026-04-02 日志

## 早上
- 早安问候 cron job 失败，错误：`⚠️ ✉️ Message failed`，consecutiveErrors: 1，判断为偶发抖动，下次自动重试

## 下午 - 重大迁移操作

### 环境排查
- 沙箱无法访问 git.sankuai.com HTTPS，但 SSH 22 端口可通
- GitHub 可正常访问
- 对外开放端口：18789(OpenClaw)、7681(ttyd)、6080(noVNC)、9223(nginx Chrome代理)、5901(VNC)

### SSH 密钥配置
- 为沙箱生成了新的 ed25519 SSH 密钥，添加到 git.sankuai.com
- 公钥：`ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIL5//Z/J9am+YOxPhCJK6n7Qknf5vRnEj43Y9IgSAPpd openclaw-sandbox`
- 已成功访问仓库：`ssh://git@git.sankuai.com/~lipengyu04/openclaw-old.git`

### OpenClaw 重启
- 旧进程 PID 148515（Mar26启动）→ 新进程 PID 252969（14:45启动），重启成功

### 大迁移：从 openclaw-old.git 同步到当前沙箱
1. **迁移前备份**：workspace 提交到 `https://github.com/zengxinyueooo/openclaw-workspace.git`（commit: `4a5d9f6`）
2. **同步的目录**（完整覆盖）：
   - `workspace/` — 人设从小桃换成**爪爪（小狐狸）**
   - `workspace-mcn-ant/bee/eagle/owl/squirrel/` — 5个子agent workspace
   - `skills/` — 新增 agent-reach、gui-agent-cloud、supabase-optimize-skills、xhs-scraper
3. **cron/jobs.json**：替换为仓库的 14 个 MCN 任务（删除了原来的下班提醒/早安问候/每日记忆沉淀）
4. **openclaw.json agents.list**：新增 5 个子 agent（mcn-ant/bee/eagle/owl/squirrel）
5. **openclaw.json 融合**（待宝宝确认写入，共 18 处变更）：
   - browser 改为 chrome-local profile
   - tools 新增 agentToAgent、exec full 权限、sessions visibility
   - hooks 新增 session-memory/command-logger/boot-md
   - agents.defaults subagents 并发从 8 提升到 20
   - 严格未动：models、channels、gateway.auth、gateway.controlUi、plugins、memorySearch

### 重要路径
- 迁移脚本 skill：`~/.openclaw/workspace/repo-sync-to-openclaw/`
- 参考 openclaw.json（宝宝本机版本）：`/tmp/openclaw-ref.json`
- 融合后待写入版本：`/tmp/openclaw-merged.json`
- 本地备份：`~/.openclaw/workspace.backup.20260402-*`

## 待完成
- [ ] 宝宝确认 openclaw.json 融合变更后写入

## 下午（16:00-17:38）- Bug 排查与修复

### 蜜蜂生图 bug 根因确认与修复
- **问题**：先叫Momo 探店笔记子需求生出了美甲图（或风格跑偏）
- **根因**：AGENTS.md 调度逻辑里查询用 `material=not.is.null`，但数据库里字段是 `material_ids`（不是 `material`），字段不存在导致蜜蜂拿不到素材，最终随机兜底选了错误素材
- **修复内容**：
  1. `workspace-mcn-bee/AGENTS.md`：调度查询改为 `material_ids=not.is.null`，素材URL从 `material_ids[0]` 查 materials 表取，内容类型从 `materials.category` 取
  2. `workspace-mcn-bee/skills/face-image-generator/generate.py`：有 `sub_requirement_id` 时素材为空直接报错终止，不再随机兜底（防止风格混乱）；无子需求ID（手动调用）保留随机兜底

### 页面进度显示 bug（已记录，未修复）
- `生图完成 image_count=0` 在页面上和真正成功的「生图完成」显示一样，会误导判断
- 后续需要在 requirement.js 的 generateTimeline 里区分 image_count=0 的失败情况

### 蜜蜂重跑情况
- 17:02 手动 spawn 蜜蜂重跑「检查常态化笔记需求并生成笔记」
- 17:33 仍在 running（已 31 分钟），疑似卡住，等皮皮决策是否 kill

### 鹰眼 AGENTS.md 确认
- 鹰眼有「手动生成需求（皮皮触发）」入口，但需要爪爪中转派活，不会自动触发
- 皮皮提出为 doudou 账号生成美甲图，待确认是否立即派鹰眼

## 傍晚（17:38-19:16）

### 蜜蜂生图 API 故障
- 蜜蜂跑了 56 分钟，找到 6 条待生图子需求（李慢慢曼妙、Momo不默默、沐沐木有烦恼、乌拉拉、小Lin晓晓、一只小糕糕）
- 生图 API `mdz.sankuai.com/api/dzusergrowth/assistant/workFlow/aiAesthetic/v2` 持续超时，600s×3次全部失败，0 张图生成
- doudou 和先叫Momo 已有图（pending 待皮皮审核），不受影响
- **待处理**：API 恢复前生图流程全部卡死，需人工确认 mdz.sankuai.com 服务状态

### 鹰眼→松鼠链路验证
- 鹰眼成功为 doudou调色盘🎨 创建 requirement（`6c8789bb`）+ sub_requirement（`f4814823`，纯美甲笔记）
- 松鼠成功填入 nail 类素材（`e0aa581c`），material_ids 写入完成
- 整条手动触发链路验证通过（鹰眼→松鼠），蜜蜂因 API 故障未能继续

### 松鼠 bug 修复
- 发现松鼠填完 material_ids 后没有写 progress
- 已修复：`workspace-mcn-squirrel/AGENTS.md` 补充了写入 progress 的步骤（RPC append）
- doudou 子需求 `f4814823` 这次松鼠填的那条 progress 漏写了，待手动补

### AGENTS.md 更新汇总（今日）
- 爪爪：新增「手动内容需求」派活流程
- 鹰眼：手动需求流程补全（description 格式、多子需求挂同一 requirement）
- 蜜蜂：调度改用 material_ids，generate.py 禁止随机兜底
- 松鼠：填 material_ids 后补写 progress

## 20:53 时区修复 & 前端部署迁移

### 问题
- sub_requirement.progress 时间戳混用 UTC（Z结尾）和 +08:00，前端 formatTime 用 timeZone:'UTC' 展示，导致时间显示错乱

### 修复内容
1. **前端**：`requirement.js` formatTime 的 `timeZone: 'UTC'` 改为 `'Asia/Shanghai'`
2. **数据库**：Python 脚本修正 64 条子需求的历史 progress 时间戳（UTC → +08:00）
3. **eagle AGENTS.md**：shell 写入时间改为 `TZ=Asia/Shanghai date +%Y-%m-%dT%H:%M:%S+08:00`
4. **bee/ant AGENTS.md**：占位符从「当前UTC时间」改为「当前北京时间，格式 2006-01-02T15:04:05+08:00」
5. **Vercel 部署**：用 API 方式（无 CLI）部署，token=`vcp_6spL1Y6EGercrXAtcFdGifXUxPVTlDmFFD29U8pM6rK8ZQmdyT2teg1z`，projectId=`prj_xuwVPZBduAxEzRPKYc1hTrsa3ge1`

### 项目迁移
- review 项目从 `~/.openclaw/workspace/tasks/supabase-materials/review/` 迁移到 `/opt/mcn-review/`
- nginx 8080 端口 root 指向 `/opt/mcn-review/`
- 访问地址：`http://33.229.112.78:8080/`（待确认皮皮是否可访问）
- 曾尝试改 openclaw gateway 到 18790+nginx 反代 18789，但 openclaw 自动重启会抢回 18789，已回滚

### 教训
- Vercel CLI 在沙箱装不上（@vercel/detect-agent 版本冲突），改用 REST API 部署
- 改 openclaw gateway 端口风险大，自动重启会覆盖配置，session 会断，不要再尝试
- nginx 8080 是对外可用端口，之前忽视了
