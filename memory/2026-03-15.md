# 2026-03-15 每日日志

## 今日对话摘要

### 学城文档编辑（Claw 学习）
- 帮宝宝整理了 km.sankuai.com/collabpage/2749830296「Claw 学习」文档
- 通过 CDP 脚本批量重命名标题：H2 加"一、二、三…"，H3 加"（一）（二）…"
- 在 Session 章节末尾新增「（四）Sub-agent Session 与 Cron Session 的生命周期」完整小节
- 文档字数从 2825 增加到 3255

### OpenClaw 知识问答
- 详细讲解了 Session 机制、记忆机制、多 Agent、Cron 的完整体系
- 重点讲解了 Sub-agent Session 生命周期（run/session/delete 三种模式）
- 解释了 `"patch required"` 报错根本原因：Agent 用了 cron.add 的格式调用 cron.update，正确格式是 `{ jobId, patch: { 只写改的字段 } }`
- 手动 cron run 空 session 原因：jobs.json 缺少 `channel` 字段导致结果无处投递

### 另一台机器 Cron 修复
- 宝宝另一台本地机器的 Cron 被 Agent 删空了（任务数=0）
- 宝宝想配置「每日AI新闻」任务，agentId=researcher（小葡🍇）
- 指出配置问题：JSON 不支持注释、缺外层 version+jobs 包裹、channel:last 不稳定、state 字段不需要手动填
- 建议改用 `channel: daxiang` + 明确 `to` 字段，停 gateway 后再编辑文件

### Agent 命名
- 宝宝在给 Agent 起水果相关的名字
- 给出了大量备选：小葡、小橘、小柠、小樱、小芒、小莓、小蓝、小蜜、小杏、小奇、小蕉、小菠、小青、小苹、小橄、小梅、小牛、小柚、小梨、小枣、小椰、小李等
- 宝宝还在选，尚未确定

### Git 提交
- 将今天网页版修改的 MEMORY.md（新增第8条行为规范）和 SOUL.md（新增任务中断处理规则）提交 push
- commit: b86de60

## 待跟进
- 另一台机器的 Cron 是否配置成功（宝宝还在调试中）
- Agent 水果名字最终选哪个
