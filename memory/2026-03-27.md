# 2026-03-27 日记

## 与宝宝的对话要点

### Sub-agent 与 MEMORY.md 加载机制讨论

- **主 Agent** 每次启动会自动加载 SOUL.md、USER.md、MEMORY.md 以及当天的 memory/YYYY-MM-DD.md（通过 Project Context 注入）
- **子 Agent（spawn 出来的）** 不会自动加载 MEMORY.md，但 workspace 的项目上下文文件（SOUL.md、USER.md、AGENTS.md、TOOLS.md、IDENTITY.md）会自动注入
- 子 Agent 没有历史记忆和长期上下文，需要主 Agent 在 spawn 时手动在 task 里带入必要背景
- 若想让子 Agent 也读 MEMORY.md，可在其独立 workspace 的 AGENTS.md 里配置，但要注意隐私风险（不适合开放给多人的场景）

### KM 文档编辑操作

- 成功将「sub-agent 不会自动加载 MEMORY.md」踩坑点写入宝宝的 Claw 学习文档（contentId: 2749830296）
- 使用 ProseMirror transaction 插入 list_item 节点到 bullet_list 末尾（offset=1000），文档自动保存成功

### Cron 任务管理

- 发现「早安问候」cron（ID: b2f04204）一直报 error，原因：task 里用 message 工具发消息但没指定 target
- 修复方式：删除旧 cron，重建时用 `--announce --channel daxiang --to user:3779912463`，让 delivery 由 cron 系统直接推送
- 新早安问候 cron ID: bf6fa341，周一到周五 10:00 触发，状态正常
- 每日记忆沉淀 cron（ID: 7b641d54）每晚 21:00 触发，状态 ok
- 下班提醒 cron（ID: 7e1cf153）工作日 20:00 触发，状态 ok
- 宝宝的大象 UID：3779912463（daxiang delivery target: user:3779912463）

### 宠物科普 Skill（pet_knowledge）

- 路径：`~/.openclaw/workspace/skills/pet_knowledge/`
- 结构：main.py、config.yml、utils/（breed_search.py、topic_mining.py、image_generation.py）、prompts/（cover_prompt.txt、content_prompt.txt）
- 原设计：用外部 run.sh 脚本生图（路径写死为 /Users/zengxinyue/... 本机路径，沙盒跑不了）
- 修复方案：直接用 catclaw-image skill（/app/skills/catclaw-image）生图，不走 subprocess 调用 run.sh
- 生图命令：`python3 /app/skills/catclaw-image/scripts/catclaw_image.py generate "prompt"`
- 封面图 prompt 模板在 prompts/cover_prompt.txt，内容图 prompt 模板在 prompts/content_prompt.txt
- 宝宝下次要用时直接问想了解哪类宠物，Agent 手动跑流程即可（不需要跑 main.py）
