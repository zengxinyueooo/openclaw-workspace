# 2026-04-03 日志

## 上午 - 审核页服务恢复

### 问题：沙箱重启后审核页不可访问
- 旧 IP `33.229.112.78` 已失效，新 IP `33.231.101.114`
- Python HTTP server 进程丢失，需重新启动

### 修复过程
1. 确认 `/mnt/openclaw/mcn-review/` 持久化目录完好
2. 临时用 `/root/mcn-review/` 起了 server（错误），后改为指向正确持久化目录
3. 更新启动脚本 `/root/.openclaw/scripts/start-review-server.sh`
4. 添加 cron job `review-server-watchdog`（每天 07:00 自动检查拉起，job id: `dbad8b3d-36eb-4574-9bbd-d1c86e86a8b4`）

### AGENTS.md 权限问题修复
- 主 workspace AGENTS.md 属主是 root，catclaw 无写权限
- 根因：昨天大迁移时主 workspace 用 root 身份 cp，子 agent workspace 用 catclaw
- 解法：`cat > /tmp/new.md && mv /tmp/new.md` 替换，mv 后属主变 catclaw
- 已成功写入审核页部署说明到 AGENTS.md

### 部署架构确认
| 组件 | 路径 |
|------|------|
| 前端文件 | `/mnt/openclaw/mcn-review/`（持久化）|
| nginx 配置备份 | `/mnt/openclaw/nginx-review.conf`（备用）|
| 启动脚本 | `/root/.openclaw/scripts/start-review-server.sh` |
| 自动恢复 | cron `review-server-watchdog` 每天 07:00 |

### 学城记录
- 创建文档《2026-04-03 沙箱环境问题排查与修复记录》
- contentId: 2754854937，链接：https://km.sankuai.com/collabpage/2754854937

### 关键教训
- `mv` 替换文件可以绕过 root 属主限制（新文件属主为执行者）
- 沙箱 IP 每次重启必变，不要在文档里硬编码 IP
- openclaw 没有 boot hook，用 cron watchdog 替代

---

## 上午 - 素材双表架构迁移（materials → available_materials）

### 背景
皮皮提出素材管理要拆成两张表：
- `materials`：原始入库、状态流转（pending/approved/rejected），松鼠采集写入
- `available_materials`：只放 approved 素材，是松鼠分配和蜜蜂查素材的唯一入口

### 数据库变更（皮皮已在 Supabase 执行）
1. `available_materials` 去掉 `assigned_to_persona` 字段（不再需要）
2. 用 `used_by TEXT[]` 同时承担预占锁 + 使用记录（一图一用，空数组=可用）
3. 建 trigger `trg_sync_approved_materials`：materials 变 approved 时自动同步到 available_materials
4. 存量补同步：239 条 approved 素材全部进入 available_materials
5. 历史已分配素材（121 条 `assigned_to_persona` 非空）同步到 available_materials.used_by

### 当前 available_materials 可用素材数量
- face: 131 条
- nail: 12 条（偏少，需补采）
- nail-face: 3 条（偏少，需补采）

### 代码变更
| 文件 | 改动 |
|------|------|
| 松鼠 `AGENTS.md` | 查素材改查 available_materials，按 category 分桶，原子 PATCH 占用（带 used_by=eq.{} 条件） |
| 蜜蜂 `AGENTS.md` | 查素材 image_url 改查 available_materials |
| `generate.py` | 查素材、随机兜底、占用标记全部改为 available_materials + used_by |

### 核心设计决策
- **一图一用**：used_by 非空即代表已占用，查询过滤 `used_by=eq.{}`
- **并发安全**：原子 PATCH 带 WHERE 条件，返回空数组说明被抢占，换下一张
- **tags 精细过滤**：预留在查询层，未来加 `tags=cs.{<tag>}` 参数
- 内存随机打乱只是候选顺序，真正去重靠数据库原子写

---

## 下午 - 素材池页面（materials-pool.html）派发功能

### 功能迭代
1. 笔记类型从 input 改为四个按钮选项（颜值日常笔记/纯美甲笔记/腿美甲笔记/颜值美甲笔记）
2. 确认派发逻辑：先插父需求（requirement），再挂子需求（sub_requirement），不经过鹰眼
   - 派发链路：页面选图选人设 → 插 requirement + sub_requirement → 蜜蜂 cron 自动生图

### Bug 修复记录
- `requirement.scene_type` 有 check constraint，只允许固定枚举值（含"常态化运营"），"手动派发"不合法
  - 修复：scene_type 用"常态化运营"，description 里写"[手动派发]"备注
- `closeDispatchModal()` 会清空 `selectedPersona/selectedCategory`，doDispatch 开头就调导致后续读 `.name` 报 null
  - 修复：doDispatch 开头先 copy 到局部变量 `persona`/`category`，再调 closeDispatchModal

---

## 下午 - AGENTS.md / NOTES.md / 鹰眼规则调整

### AGENTS.md 补充说明
- 新增"📁 沙箱持久化路径说明"章节，注明仅适用沙箱环境（sandbox-ide-*）
- 修改 AGENTS.md、SOUL.md、MEMORY.md 等文件必须用 `/mnt/openclaw/.openclaw/workspace/` 路径

### NOTES.md 调整
- 暂停优先级规则（工作流未建立），只保留颜值/美甲两类运营
- 商单字段规则：默认 false，皮皮主动告知才设 true，agent 不自行判断
- 原优先级规则和未启用工作流类型已备份在文件底部注释里

### 学城文档命名规则
- 格式：`MM-DD 干什么`，不写年份（已写入 MEMORY.md）

### available_materials 字段讨论
- 核心必须字段：`category`、`tags`、`style`、`score`、`source_keyword`
- 可选字段：`mood`、`face_ratio`、`sharpness`、`resolution`
- 待皮皮确认 tags/style 的打标维度（颜值：气质/风格；美甲：款式/颜色/季节）

---

## 下午 - 蜜蜂 token 消耗过高排查

### 问题
蜜蜂 sessions 总 token 消耗超过 1,030k，皮皮发现后要求排查。

### 根因
1. **生图 API（aiAesthetic）持续返回 0 张图片**：Google/Gemini 后端故障，每次调用返回空结果
2. **脚本内有 4 轮重试，单轮 600s 超时**：每个 subagent 跑 20+ 分钟才判定失败
3. **等待期间 process(poll) 轮询频繁**：每 30 秒一次，产生大量 token 消耗
4. **cron 一次并行 spawn 5 个 subagent**：5 个都失败，token 叠加
5. **cron 触发频率偏高**：产生了 15 个 cron run session（`ralph-loop-bee`）

### 蜜蜂 subagent 强制停止
- 通过修改 `sessions.json` 将活跃 subagent 标记 `abortedLastRun=true`
- kill 了残余生图 curl 进程（PID 265846、265847、265848）

### 建议修复方向（待实施）
- 生图失败 2 轮后熔断，不等满 4 轮
- 第一个 subagent 回报 0 图时停止 spawn 剩余
- 用 `exec(yieldMs=30000)` 替代 `process(poll)` 减少 token
- 降低 cron 触发频率

---

## 下午 - 沙箱写路径铁律确立

### 问题
爪爪反复把文件写到 `/root/.openclaw/workspace/` 而不是 `/mnt/openclaw/.openclaw/workspace/`。

### 根因
OpenClaw 启动时把 `/root/.openclaw/workspace/` 注入 system prompt，导致爪爪每次的"直觉"都是往那里写。

### 修复
在 `/mnt/openclaw/.openclaw/workspace/AGENTS.md`「沙箱持久化路径说明」一节加入强制规则：
- `/root/.openclaw/` 被注入只是"让我能读"，不代表能写
- 所有写操作必须且只能用 `/mnt/openclaw/.openclaw/` 路径
- 每次写文件前强制检查路径前缀

### 关键记忆
**读哪里都行；写只走 `/mnt/openclaw/.openclaw/`，不走 `/root/.openclaw/`，没有例外。**

---

## 下午 - AGENTS.md 被意外覆盖并从 git 恢复

### 问题
爪爪今天修改 AGENTS.md 时，误用 `/root/.openclaw/workspace/AGENTS.md`（OpenClaw 注入的残缺通用版）作为底稿，覆盖了皮皮自定义的完整版本（含 MCN 调度规则、各 agent 委派流程等）。

### 根因
`/root/.openclaw/workspace/` 被 OpenClaw 注入 system prompt，导致爪爪"直觉"上把它当成可写的主路径。实际上沙箱里的可写路径是 `/mnt/openclaw/.openclaw/workspace/`。

### 修复
从 git 历史（commit `5c2f2b2`）恢复完整版，并补入今天新增的两段内容（养号委派规则 + 沙箱写路径铁律）。恢复后 commit `8a887df`。

### 教训
**改文件之前，永远先确认操作的是 `/mnt/openclaw/.openclaw/` 下的文件。** AGENTS.md 属主恢复为 catclaw，后续可直接用 `edit` 工具，不需要绕 `/tmp`。

---

## 晚上 - 养号任务创建（清明节选题）

### 背景
皮皮要为7个账号创建清明节相关养号任务，时长2天（4月4日-5日）。

### 账号与 account_id
| 账号 | ID |
|------|-----|
| 被夸会脸红 | 32 |
| 高冷汉堡包 | 31 |
| 还没人设 | 29 |
| 腿短但跑得快 | 28 |
| 体温偏高 | 26 |
| 旺仔小拳头 | 25 |
| 全糖去冰 | 23 |

### 关键词分配（每账号每天不重复）
- 清明节去哪玩 / 清明节旅游攻略 / 清明节短途旅行 / 清明节出行推荐 / 清明小长假攻略 / 五一假期去哪玩

### 执行结果
- MOA **在沙箱可用**，`mtsso-moa-local-exchange --audience 055da5ec53` 直接拿到 token ✅
- 14条任务全部创建成功（task ID 44~57），start_time=2026-04-03 19:42，duration=1
- duration=1 含义：**不是跑1天**，而是当天生成6条 atomicTask，今晚跑完
- status=3（执行中），6条子任务分布在 19:12~21:55 北京时间

### 最终结果
- 明天（04-04）14条任务创建成功，task ID 60~66，start_time=2026-04-04 18:00 ✅
- 后天（04-05）14条任务创建成功，task ID 67~73，start_time=2026-04-05 18:00 ✅
- 4月4日/5日 cron `ant-account-nurturing` 是否暂停待皮皮确认

### skill 修复
- `account_nurturing_cli.py` 加了 `_normalize_start_time()` 函数，自动把 `"2026-04-04 18:00:00"` 转成 `"2026-04-04T18:00:00"`，修复了 start_time 格式导致的 10001 系统错误

---

## 下午 - 养号 cron 配置

### 新增 cron job `ant-account-nurturing`
- **job id**：`348cc335-fe60-43aa-a403-06c863767eaf`
- **agentId**：`mcn-ant`（蚂蚁执行）
- **调度**：每天 09:00 北京时间
- **message**：「执行养号任务」（一句话）
- **delivery**：none

### 同步更新
- 蚂蚁 `AGENTS.md`：新增养号任务章节（读 skill → 查状态/列表 → 按需创建 → sessions_send 汇报）
- `skills` 表补充 `account-nurturing` 条目
- 爪爪 `MEMORY.md`：蚂蚁职责加「养号」，新增养号委派规则段落

---

## 下午 - 素材采集结果

松鼠采集任务执行结果：
- face（默认关键词）：60 条入库 ✅
- face（甜妹/韩系/怼脸拍）：10 条入库 ✅
- nail：0 条 ⚠️（详情页 404 / 登录态可能过期）
- 总计：70 条 face 素材，status=pending

nail 采集失败待确认：Chrome 里小红书是否需要重新登录。

---

## 下午 - shared/personas 账号数量确认

皮皮询问 shared/personas 是否有 8 个账号，实际只有 **7 个**：
Momo不默默、一只小糕糕、乌拉拉、先叫Momo、小Lin晓晓、李慢慢曼妙、沐沐木有烦恼

第 8 个账号尚未建立，待皮皮确认。

---

## 下午 - marketing-content-serivce 仓库克隆与环境搭建

### 仓库克隆
- 参考文档：https://km.sankuai.com/collabpage/2754393701（昨天写的迁移实录）
- 克隆命令：`git clone ssh://git@git.sankuai.com/beauty/marketing-content-serivce.git /mnt/openclaw/repos/marketing-content-serivce`
- SSH key 已有效（`~/.ssh/id_ed25519`），22 端口可达，克隆成功

### 环境搭建
- 项目要求 Python ≥ 3.13，使用 conda 创建独立环境
- Conda 安装：`/mnt/openclaw/miniconda3/`（持久化目录）
- Conda 环境：`mcservice`（Python 3.13.12）
- Poetry 安装到 conda env：`/mnt/openclaw/miniconda3/envs/mcservice/bin/poetry`（2.3.3）
- 依赖安装：`poetry install --no-root`（virtualenv 禁用，直接装入 conda env）
- 额外手动安装：`torch==2.7.1+cpu` + `torchvision==0.22.1+cpu`（pyproject.toml 未声明 torch，ultralytics 依赖它）

### Bug 修复
- `src/infrastructure/image/filters/mosaic.py` 中 `MODEL_PATH` 硬编码了作者本机 macOS 路径
- 修复：改为动态解析 `_pathlib.Path(__file__).resolve().parents[4] / "model" / "face_landmarker.task"`
- 模型文件已在仓库 `model/face_landmarker.task`，修复后正常加载

### Smoke Test 结果
- 流程链路完全跑通，Mermaid 工作流图已生成保存到 `docs/workflow_graph.md`
- 唯一 block：调用 `aigc.sankuai.com` 返回 `400 配置不存在`
  - 原因：默认 `AIGC_BEARER_TOKEN=2001903587361988625` 在当前沙盒无效/无权限访问 `gemini-3-pro-preview`
  - 需皮皮提供有效 AIGC Bearer Token，通过 `AIGC_BEARER_TOKEN=<token>` 环境变量注入

### 关键路径
| 组件 | 路径 |
|------|------|
| 仓库 | `/mnt/openclaw/repos/marketing-content-serivce` |
| Python | `/mnt/openclaw/miniconda3/envs/mcservice/bin/python` |
| 运行测试 | `cd 仓库目录 && AIGC_BEARER_TOKEN=<token> python src/tests/test_ai_aesthetic_workflow_smoke.py` |

---

## 晚上 - marketing-content-serivce Smoke Test 跑通

### 结果
- 皮皮提供了有效 AIGC Bearer Token 后，完整流程跑通
- `success=true`，审美评分 **0.78**，仲裁通过
- 生图耗时约 100s，全流程 207s
- 生图地址：`https://s3plus.vip.sankuai.com/aigc-public-resources/gemini-image-generate/69cf8e08-e4b0686c-d13123d9-1775209992924.png`

### 已知 Warning（不影响主流程）
- `yolov8n-face.pt` 缺失（`models/` 目录下没有）→ 人脸检测跳过
- `insightface` 未安装 → 人脸相似度检测跳过

---

## 晚上 - 颜值生图 V4 Skill 创建

### 背景
皮皮要求：基于跑通的 ai_aesthetic_workflow，写一个蜜蜂生图时调用的新 skill，替代原有 V3 generate.py（通过 API 调用后端）。

### 产出
- `skills/face-image-generator/SKILL_V4.md` — Skill 文档
- `skills/face-image-generator/run_v4.py` — 执行脚本

### 参数来源对照
| 参数 | 来源 |
|------|------|
| `task_name` | `sub_requirement.persona_name` |
| `style_reference_image` | `available_materials.image_url`（从 `sub_requirement.material_ids[0]` 查得）|
| `portrait_reference_picture` | `shared/personas/<name>/profile.json` → `persona.visual.portraitReference` |
| `pic_num` | 默认 3 |
| `image_size` | 固定 `"1K"` |
| `aspect_ratio` | 固定 `"3:4"` |
| `mask_image` | 不传 |

### 调用方式
```bash
/mnt/openclaw/miniconda3/envs/mcservice/bin/python \
  skills/face-image-generator/run_v4.py \
  --persona 小Lin晓晓 \
  --sub-requirement-id <uuid> \
  --style-url <available_materials.image_url> \
  --pic-num 3
```

### AGENTS.md 同步更新
- Skills 表入口改为 `SKILL_V4.md`
- 手动派活流程改为调 `run_v4.py`
- 环节1 face 分支改为 `run_v4.py --pic-num 8`，加 conda 路径警告
- 美甲（nail/nail-face）逻辑不变

### Bug 修复
- `run_v4.py` 写库时多了 `source: "v4-workflow"` 字段，但 `generated_images` 表没有此列 → 已删除
- 第一次跑乌拉拉生图：素材无人脸（MediaPipe 检测不到）→ 换素材后成功生 8 张
- 生图两次导致库里有 16 条记录（第一次部分写入后报错，第二次重跑又写 8 张）

### 审核页参考图说明
- 审核页查 `generation_tasks.style_url` 展示参考图，是页面故意加的对比展示
- 不是 generated_images 里写了参考图，`run_v4.py` 不写 generation_tasks 表，所以 style_url 查不到

### 审核页风格参考图缺失 Bug（待修）
- 现象：V4 生图任务在审核页只显示人设参考图，无风格参考图
- 根因：`image-select.js` 第65行从 `generation_tasks.style_url` 查，run_v4.py 不写该表
- 待皮皮确认修复方案：
  - A（推荐）：`generated_images` 加 `style_url` 字段，run_v4.py 写入，页面从这读
  - B（快）：页面从 `sub_requirement.material_ids[0]` → `available_materials.image_url` 兜底

---

## 晚上 - 蜜蜂流程梳理（V4 后）

颜值生图链路完成切换：
- face：`run_v4.py`（本地 ai_aesthetic_workflow + Gemini）
- nail/nail-face：`nail-image-generator/generate.py`（不变）
- pic_num 默认改为 8
- python 路径必须用完整 conda 路径：`/mnt/openclaw/miniconda3/envs/mcservice/bin/python`

### Skill 文件整理
- `face-image-generator/SKILL.md`（旧，对应 generate.py）已删
- `generate.py` 已删
- `颜值生图.md` 改名为 `SKILL.md`，是当前唯一入口
- AGENTS.md 中所有引用路径同步更新为 `skills/face-image-generator/SKILL.md`

### 职责边界确认
- `used_by` 标记是松鼠的职责（填 material 时已占住），蜜蜂不动
- 蜜蜂生图只做：写库 `generated_images` + 追加 `sub_requirement.progress`
- `run_v4.py` 中 used_by 更新代码已删除

---

## 晚上 - 今日工作记录写入学城
- 文档：「04-03 MCN 系统全天工作实录」
- contentId: 2755075609
- 链接：https://km.sankuai.com/page/2755075609

## 晚上 - 清明节养号任务汇总
- 今天（04-03）：task ID 44~57，已在执行中
- 明天（04-04）：task ID 60~66，18:00 开始
- 后天（04-05）：task ID 67~73，18:00 开始
- skill 修复：`_normalize_start_time()` 修复 start_time 格式问题（空格→T）
- 待办：4月4/5日 cron `ant-account-nurturing` 09:00 是否暂停，皮皮未确认

---

## 深夜 - 生图统计体系建立

### 新表 image_gen_log（已建）
| 字段 | 说明 |
|------|------|
| workflow | 来源标识：ai_aesthetic / nail_leg / nail_hand / nail_both |
| persona_name / sub_requirement_id | 单独列，方便聚合统计 |
| success / image_count / duration_seconds / error_type | 核心指标 |
| params / result | jsonb，各工作流自定义，不固定字段 |

### 公共 logger
- 路径：`/mnt/openclaw/repos/marketing-content-serivce/src/utils/image_gen_logger.py`
- 用法：`from utils.image_gen_logger import log_image_gen`，调一行，失败静默不影响主流程
- 美甲脚本通过 `sys.path.insert(0, SERVICE_SRC)` 引入，不需要改 repo 之外的配置

### 脚本改造
- `run_v4.py`（颜值）：已加 `time.time()` 计时 + 调 log_image_gen，成功/失败都记录
- `generate.py`（美甲）：重构返回值为三元组 `(result, saved_count, error_msg)`，不再 `sys.exit`，main() 统一写 logger + progress

### error_type 标准值
`no_face` / `api_empty` / `http_500` / `http_429` / `timeout` / `workflow_error` / `api_error` / `write_failed`

---

## 深夜 - 生图统计页面上线

- 新页面：`gen-stats.html`（生图成功率统计）
- 功能：KPI卡片 + 成功率趋势图 + 错误类型饼图 + 各工作流对比表 + 明细日志
- 时间粒度：日度（近30天）/ 周度（近8周）/ 月度（近3个月），当前周期永远在最右
- 数据源：`image_gen_log` 表
- 修复：config.js 用 `SUPABASE_DEFAULTS.url`，不是 `AppConfig.supabaseUrl`
- 修复：配置表单默认隐藏，自动连接，连接失败才显示表单
- 所有其他页面导航已加入"生图统计"入口
- Vercel token 已存入 `/mnt/openclaw/.openclaw/workspace-mcn-bee/.env.supabase`
