
## 10:47 需求追踪页面"父需求有标题但子需求为0"排查

### 问题
需求追踪页面显示 4.6 的 requirement（b03f79a7）有父需求标题但子需求数量为 0，4.7 当天则连父需求都没有创建。

### 根因
`daily-eagle-daily-check` cron（每天北京时间 4:00）触发时，爪爪 spawn 鹰眼的 task 描述太模糊：
> "执行账号数据检查：查 note_analytics 表...若某账号本周浏览量>100的笔记数量<1，则标记为需要常态化运营"

鹰眼靠自由发挥判断要干什么：
- 4.6（session 2fe95cf2）：顺手创建了父需求，但漏建子需求，直接汇报结束
- 4.7（session 4ec25747）：做完检查汇报"待命等指示"，父需求也没建

### 修复
AGENTS.md 第97行 task 改为：
> "执行颜值账号日巡检：按 AGENTS.md「颜值账号日巡检」流程，查 note_analytics 表检查所有颜值+美甲账号本周数据，命中账号创建 requirement + sub_requirement，最后 sessions_send(label='main') 汇报结果"

明确指向 AGENTS.md 流程，不依赖鹰眼自行推断。明天 4.8 04:00 验证效果。

### 教训
spawn task 描述必须说清楚"做完要写入什么"，不能只说"检查"，否则 agent 行为不稳定。

---

## 11:00 no_face 生图失败无限循环 + 修复

### 问题
生图审核页大量 `no_face` 报错。共 44 条失败记录，100% 来自两张风格参考图：
- 小Lin晓晓：低头+闭眼，MediaPipe FaceMesh 找不到正面轮廓
- Momo不默默：侧脸+强逆光，面部遮挡严重

失败后 `sub_requirement.status` 不变（仍为 pending），`generated_images` 无记录，蜜蜂下次 cron 照样捞到 → 无限循环。

### 修复（已落地）
**run_v4.py**：no_face 失败时在 `sub_requirement.progress` 追加带 `error_type: "no_face"` 和 `style_url` 的失败记录。

**蜜蜂 AGENTS.md 新增调度步骤 3.5**：
- `no_face_count >= 2` 且偶数 → 从 available_materials 换一张新素材（排除已失败过的），更新 material_ids
- 找不到可用素材 → `status=blocked`
- `no_face_count >= 6` → 强制 blocked

### 现状
两个账号子需求均为 obsolete（之前已被手动标记），新换图机制从下次新子需求开始生效。

### 教训
生图失败路径必须有状态流转（写失败标记），否则调度层感知不到，会无限重试。

---

## 11:05 鹰眼创建 4.7 常态化运营需求

手动触发鹰眼补创今日需求：
- 18 个账号中 17 个命中（小Lin晓晓被误判为达标——实为同一篇笔记被采集两次，鹰眼未去重）
- 创建 1 条 requirement（9b170def），17 条 sub_requirement，全部 pending
- 松鼠同日采集 70 条 face 素材（nail 仍 0 条）

**鹰眼巡检去重 bug**：`note_analytics` 按 `persona_name` 统计 views>100 记录数，但同一篇笔记每天采集一次会产生多条记录，导致把"1篇笔记被采了2天"算成"2篇达标笔记"。需要按 `note_title` 或笔记ID去重后再统计。

---

## 11:30 生图审核页删除任务后刷新复现 bug 修复

### 根因
删除操作顺序：先删 `generated_images`，再更新 `sub_requirement.deleted_at`。
第二步偶发失败 → 图删了但 deleted_at 没写入 → 刷新后任务重新出现（空任务，图没了）。

### 修复
改为先写 `deleted_at` 再删图，顺序调换。已部署 Vercel，commit bbfa8f9。

---

## 14:46 生图审核页参考图简化：只留人物参考 + 风格参考

素材原图和风格参考内容一样，去掉重复展示。`renderRefImages` 改为只接受两个参数，同时去掉了 `material_ids → materials` 的 DB 查询（节省一次请求）。已部署 Vercel + 同步本地 8080。

---

## 14:55 task_id 现在的用途

`task_id` 是 `generated_images` 表的外键，指向 `generation_tasks.id`。
目前唯一用途：查 `generation_tasks.style_url` 作为风格参考图。
- 1453 张有 task_id（新批次），421 张没有（旧批次）
- 大部分 style_url 是 webp，Gemini 会拦截
- 子需求表没有 `style_url` 字段

---

## 15:46 V4 生图缺少 AI 味检测和美感打分

### 问题
新生成的图 `ai_taste_score` 和 `aesthetic_score` 全是 null，审核页不显示"AI味"标签和"已通过/AI味过重"分类。

### 根因
`run_v4.py` 是重写版，虽然调用了 `ai_aesthetic_workflow` 并写入了 `aesthetic_score`（但实测也是 null），完全没有 AI 味检测模块。旧链路的 AI 味检测是独立步骤，V4 迁移时漏掉了。

### 待修
两个方案：
1. 在 `run_v4.py` 补 AI 味检测模块（score≥60 → `status=ai_rejected`）
2. 用 Gemini 同时评"是否有明显 AI 感"（复用现有 API）
方案未定，等皮皮确认。

---

## 14:16 生图审核页参考图不显示修复

### 问题
生图审核页任务详情里"人物参考图"和"风格参考图"一直不显示。

### 根因
参考图代码从 `generation_tasks.portrait_url` 取，但 421 张旧图的 `generated_images.task_id = null`，
无法关联 `generation_tasks`，导致参考图全部取不到。

### 修复
`image-select.js` 改为三路来源：
1. **人物参考**：内嵌 `PERSONA_PORTRAIT` 字典，按 `persona_name` 直接命中（来自 `workspace/shared/accounts/*/profile.json` 的 `persona_portraitRef` 字段）
2. **素材原图**：从 `sub_requirement.material_ids` → `materials` 表查第一张
3. **风格参考**：`generation_tasks.style_url`（新批次才有）

### 重要发现
- 人设参考图的权威来源是 `workspace/shared/accounts/<人设名>/profile.json` 的 `persona_portraitRef`
- 蜜蜂的 `workspace-mcn-bee/shared/personas/` 是老目录，数据不全，后续可清理
- 16 个颜值账号全部有 `persona_portraitRef`，doudou调色盘🎨 是美甲账号不需要人物参考图
- 已部署 Vercel


## 14:15-15:12 账号管理 & 素材池更新

### 账号盘点
- 全部账号共 23 个，颜值 17 个、美甲 1 个（doudou）、其他类型 5 个
- **profile 完整的颜值账号（3个）**：一只小糕糕、小Lin晓晓、李慢慢曼妙
- **补充了参考图（persona_portraitRef）的账号（9个）**：
  - 小Lin晓晓（链接纠正）
  - 被夸会脸红、体温偏高、腿短但跑得快、糯糯糖要努力、增增哇嘎奶、全糖去冰、高冷汉堡包、旺仔小拳头
- **暂时跳过运营（无参考图）**：爱美的豆浆妈妈、还没人设、doudou调色盘🎨
- 写入鹰眼 AGENTS.md：巡检时跳过 `persona_portraitRef` 为空的账号

### 素材池问题
- 近几批松鼠采集的素材全是 `pending` 状态，素材池只展示 `approved`，所以显示为空
- 根因：ZHIPU_API_KEY 未配置，AI 打标失效，所有素材评分为 0，没有自动 approve 逻辑
- **暂未修复**，待皮皮决定处理方式（手动审核 / 批量 approve / 配置 ZHIPU_API_KEY）

### 素材池页面更新
- `materials-pool.html` 人设列表从 8 个扩充到 18 个（全部颜值+美甲账号）
- 旧：沐沐/doudou/李慢慢/Momo/乌拉拉/先叫Momo/一只小糕糕/小Lin晓晓
- 新：以上 + 爱美的豆浆妈妈/全糖去冰/旺仔小拳头/体温偏高/糯糯糖要努力/腿短但跑得快/还没人设/增增哇嘎奶/高冷汉堡包/被夸会脸红
- 已同步部署到 Vercel（review-gold-seven.vercel.app）

### VERCEL_TOKEN
- 已存入 `/mnt/openclaw/.openclaw/workspace/.env.supabase`
- 后续 vercel 部署直接 source 这个文件即可

---

## 15:44 生图审核页"满8张才进审核"

### 变更
`image-select.js` 过滤条件改为 `face_passed_count >= 8 && pending_review_count > 0`，不满 8 张的任务不显示在审核页。已部署 Vercel（commit ae29d42）。

### 各任务状态（截至15:44）
- ✅ 满8张（8个）：doudou调色盘🎨、一只小糕糕、乌拉拉、全糖去冰、李慢慢曼妙、沐沐木有烦恼、被夸会脸红、糯糯糖要努力
- ⚠️ 不足8张：体温偏高(7)、旺仔小拳头(5)、高冷汉堡包(7) → 蜜蜂下次补生8张
- 🔄 换素材中：Momo不默默、先叫Momo（no_face失败）
- ❌ 内容安全拦截：增增哇嘎奶（素材露胸被Gemini拦截）→ 已删除问题素材，换新素材
- ⏳ 等生图：腿短但跑得快

---

## 15:10 蜜蜂生图调度逻辑简化

AGENTS.md 分类逻辑从5条简化为3条：
- 有 approved → 环节2（生草稿）
- face_passed 数量 >= 8 → 跳过
- 其他 → 环节1，每次生 8 张（不做补生数量精细化控制）

---

## 16:41 账号改名

| 旧名 | 新名 | 备注 |
|------|------|------|
| 还没人设 | 今天想吃葡萄🍇 | 新增参考图 p0.meituan.net/...ede4939...png |
| 爱美的豆浆妈妈 | 椰椰椰即 | 参考图不变 |

已同步：shared/accounts/ 目录名+profile.json、_index.json、蜜蜂 personas/、sub_requirement 表 persona_name。
